version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: serverless-saas-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: serverless_saas
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: serverless-saas-redis
    ports:
      - "6379:6379"

  lakehouse:
    build:
      context: ./apps/lakehouse
      dockerfile: Dockerfile
    container_name: saarthi-lakehouse
    ports:
      - "8001:8001"
    environment:
      DELTA_PATH: /data/pension_records
    volumes:
      - lakehouse_data:/data
    restart: unless-stopped

volumes:
  postgres_data:
  lakehouse_data:
