import { notFound } from 'next/navigation'

// FEATURE_FLAGS.plans = false — entire plans section is disabled.
// Remove this file when re-enabling the feature.
export default function PlansLayout() {
    notFound()
}
