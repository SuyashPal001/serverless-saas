import { notFound } from 'next/navigation'

// FEATURE_FLAGS.board = false — Board section is disabled.
// Remove this file when re-enabling the feature.
export default function BoardLayout() {
    notFound()
}
